import type { Metadata } from "next";
import { Inter, Geist } from "next/font/google";
import "./globals.css";
import { cn } from "@/lib/utils";

const geist = Geist({subsets:['latin'],variable:'--font-sans'});

const inter = Inter({
  subsets: ["latin"],
  variable: "--font-inter",
  display: "swap",
});

export const metadata: Metadata = {
  title: "MineConnect – Mining Industry Platform",
  description:
    "MineConnect is the all-in-one platform for mining operations – supplier management, contractor compliance, recruitment, community, and AI-powered insights.",
  keywords: "mining, suppliers, contractors, compliance, recruitment, mining industry platform",
  openGraph: {
    title: "MineConnect – Mining Industry Platform",
    description: "Connecting the mining industry through technology.",
    type: "website",
  },
};

export default function RootLayout({
  children,
}: Readonly<{
  children: React.ReactNode;
}>) {
  return (
    <html lang="en" className={cn("h-full", inter.variable, "font-sans", geist.variable)}>
      <head>
        <link rel="preconnect" href="https://fonts.googleapis.com" />
        <link rel="preconnect" href="https://fonts.gstatic.com" crossOrigin="" />
      </head>
      <body className="min-h-full">{children}</body>
    </html>
  );
}
